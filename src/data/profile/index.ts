export interface Profile {
  id: string;
  name: string;
}

export type ProfileType = {
  id: string;
  name: string;
  nameKana: string;
  gender: string;
  phone: string;
  postalCode: string;
  address: string;
  dateOfBirth: string;
  biography: string;
};

// export type ProfileModalType = {
//   open: any;
//   onClose: any;
// };
